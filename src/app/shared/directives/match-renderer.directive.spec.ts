import { MatchRendererDirective } from './match-renderer.directive';

describe('MatchRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchRendererDirective();
    expect(directive).toBeTruthy();
  });
});

import { TypeInRendererDirective } from './type-in-renderer.directive';

describe('TypeInRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeInRendererDirective();
    expect(directive).toBeTruthy();
  });
});

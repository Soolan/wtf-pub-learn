import { ParserDirective } from './parser.directive';

describe('ParserDirective', () => {
  it('should create an instance', () => {
    const directive = new ParserDirective();
    expect(directive).toBeTruthy();
  });
});

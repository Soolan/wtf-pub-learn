import { CardRendererDirective } from './card-renderer.directive';

describe('CardRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new CardRendererDirective();
    expect(directive).toBeTruthy();
  });
});

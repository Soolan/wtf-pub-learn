import { BlackRendererDirective } from './black-renderer.directive';

describe('BlackRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackRendererDirective();
    expect(directive).toBeTruthy();
  });
});

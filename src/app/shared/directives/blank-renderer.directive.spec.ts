import { BlankRendererDirective } from './blank-renderer.directive';

describe('BlankRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new BlankRendererDirective();
    expect(directive).toBeTruthy();
  });
});

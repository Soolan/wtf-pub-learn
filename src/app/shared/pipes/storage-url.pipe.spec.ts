import { StorageUrlPipe } from './storage-url.pipe';

describe('StorageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new StorageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
